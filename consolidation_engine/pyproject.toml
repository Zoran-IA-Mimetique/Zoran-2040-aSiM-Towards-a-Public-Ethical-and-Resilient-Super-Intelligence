[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "zoran-consolidation-engine"
version = "1.0.0"
description = "ZORAN_CONSOLIDATION_ENGINE_V1 — robot déterministe de consolidation (dry-run strict, plan canonique v1.1 §15)"
requires-python = ">=3.9"
license = {text = "MIT"}

[project.scripts]
zce = "zce.cli:main"

[tool.setuptools]
packages = ["zce"]
