// Échantillon gelé — adaptateur cœur→UI (plan §7).
// Cible WIRE : les reçus ne sont pas encore affichés côté interface.

export function adaptCoreResponse(response: unknown): unknown {
  return response;
}
